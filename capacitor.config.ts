import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'angular-mobile-test',
  webDir: 'dist/angular-mobile-test',
  bundledWebRuntime: false
};

export default config;
