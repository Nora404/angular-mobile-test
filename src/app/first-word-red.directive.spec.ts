import { FirstWordRedDirective } from './first-word-red.directive';

describe('FirstWordRedDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstWordRedDirective();
    expect(directive).toBeTruthy();
  });
});
